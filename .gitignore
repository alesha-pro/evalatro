# deps & build
node_modules/
dist/

# secrets
.env
.env.local
*.key

# user-specific config (copy balatro.config.example.json → balatro.config.json)
balatro.config.json

# web app build & deps (also covered by dist/ + node_modules/ — explicit for clarity)
web/dist/
web/node_modules/

# generated data
logs/
bench/*.db
bench/*.db-wal
bench/*.db-shm
bench/last-results.json
*.tsbuildinfo
